// Build phases: enable features as they ship
export const FEATURES = {
  gamification: true,
  firebase: true,
  allCourses: true,
  sequentialUnlock: true,
  katex: true,
  animations: true,
} as const
